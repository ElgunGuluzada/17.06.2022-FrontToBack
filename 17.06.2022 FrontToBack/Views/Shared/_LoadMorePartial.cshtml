@model List<Product>

    @foreach (var item in Model)
     {
          <div class="col-sm-6 col-md-4 col-lg-3 mt-3">
             <div class="product-item text-center" data-id="@item.Category.Name.ToLower()">
                <div class="img">
                    <a asp-controller="home" asp-action="detail" asp-route-id="@item.Id" asp-route-name="@item.Name">
                        <img src="~/img/@item.ImageUrl" class="img-fluid" alt="">
                    </a>
                </div>
                <div class="title mt-3">
                    <h6>@item.Name</h6>
                </div>
                <div class="price">
                    <span class="text-black-50">Add to cart</span>
                    <span class="text-black-50">$@item.Price</span>
                </div>
            </div>
          </div>
     }

    @* @for (var i = 0; i < @Model.Count(); i++)
    {
        <div class="col-sm-6 col-md-4 col-lg-3 mt-3">
             <div class="product-item text-center" data-id="@Model[i].Category.Name.ToLower()">
                <div class="img">
                    <a asp-controller="home" asp-action="detail" asp-route-id="@Model[i].Id" asp-route-name="@Model[i].Name">
                        <img src="~/img/@Model[i].ImageUrl" class="img-fluid" alt="">
                    </a>
                </div>
                <div class="title mt-3">
                    <h6>@Model[i].Name</h6>
                    <h5 class="lastData">@Model[Model.Count()-1].ImageUrl</h5>
                </div>
                <div class="price">
                    <span class="text-black-50">Add to cart</span>
                    <span class="text-black-50">$@Model[i].Price</span>
                </div>
            </div>
          </div>
    }*@